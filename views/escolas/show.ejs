<h1>Escola</h1>

<a href="/escolas/add" class="btn btn-success mb-3">Add Escola</a>
<table class="table table-bordered">

    <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        <% escolas.forEach(curso=> { %>
            <tr>
                <td>
                    <%= escola.id %>
                </td>
                <td>
                    <%= escola.nome %>
                </td>
                <td>
                    <a href="/escolas/edit/<%= escola.id %>" class="btn btn-primary btn-sm">Editar</a>
                    <form action="/escolas/delete/<%= escola.id %>" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                    </form>
                </td>
            </tr>

            <% }) %>
    </tbody>
</table>